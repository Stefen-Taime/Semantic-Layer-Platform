# Trino Configuration

This folder documents the main SQL entry point of MetricForge NYC.

Trino is the preferred engine to:

- query certified tables registered in Hive,
- serve the queries generated by the metrics engine,
- expose analytical SQL that stays close to expected lightweight-production behaviour.

The `catalog/` subfolder contains a minimal Hive catalog example to get started.
